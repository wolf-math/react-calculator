import React from 'react';

import './numpad.scss';

import Button from '../button/button';

const Numpad = (props) => {
  return (
    <div className="numpad">
      <Button buttonName="1" />
    </div>
  )
};

  export default Numpad;